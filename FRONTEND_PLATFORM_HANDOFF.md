# 年会官网与一体化平台：前端适配交接

交接日期：2026-09-08。状态：本轮页面视觉与交互原型阶段性确认，进入平台适配和前后端联调；尚未作为正式业务系统上线。

## 2026-09-09 视觉更新

本次将确认后的暖褐视觉建议版同步到官网预览。新增 `assets/brand-visual.css`，由 `index.html` 在原样式之后加载；须随完整仓库一并拉取。

- 延续长江黄河主视觉的墨色、暖褐与纸色，统一公开页面、学习护照、账户和报名的视觉层。
- 小场域交流式深研课卡片、账户“我的报名”卡片、钱包“做任务赚币”按钮统一使用 `#826D5E` 背景；文字使用不透明白色，必要的阶段标签用深褐底。
- 主视觉图片、原文字、DOM 顺序、角色权限、表单字段、路由与交互脚本不变；日程颜色单独隔离，首页完整护照样例继续隐藏。
- 此样式层用于独立官网预览。适配一体化平台时请按组件隔离规则和迁移语义 token，不直接注入平台全局样式。旧 `--blue` 等变量名仅为兼容桥接，不代表新设计仍使用蓝色。
- 本次发布仍是前端原型，不代表真实报名、支付、充值、权限与接口已经上线。未追踪的内部笔记、旧方案与临时截图不在本次提交范围。

**展示范围补充（2026-09-08）：** “学习方式”下方的整块学习护照交互预览暂时隐藏，待完整设计完成并经业务确认后，再向用户展示。保留上方学习方式介绍，不删除护照代码与能力。本次只隐藏首页嵌入预览，一键查询内的学习护照相关入口保留供适配、联调；保留原型不代表已批准正式开放。

本文对应本次提交中的完整代码。请在接收时记录 `git rev-parse HEAD`，以同一版本的代码和预览验收。此前 `README.md`、`KIMI_FRONTEND_HANDOFF.md` 的旧版说明只作历史参考，不沿用其中的单文件、三类票种、旧板块及旧截止规则。

## 1. 这次需要完成什么

以现有官网为内容、视觉和交互基准，按一体化平台现有技术栈完成组件化、样式隔离、路由与登录衔接，并与后台接入真实数据。不需要重新设计一套官网。

已确认的文案、板块顺序、票种顺序及主要交互保留。平台适配可以调整必要的组件结构和技术实现；涉及用户流程、业务规则、内容或视觉的明显变化，先提供差异给业务确认。

**当前交付的是可运行的前端原型，不等于只换 API 地址就能上线。** 登录、真实订单、支付、选座、酒店、发票、课程库存、媒体服务等，仍需接入已有页面或补齐正式界面。此文不承诺现有平台已经提供任何具体接口。

## 2. 交付物与运行方法

- [线上预览](https://leariceleto.github.io/eicc-2026-preview/)
- [Gitee 完整代码](https://gitee.com/dett-tech/eicc-2026-preview)，`main` 分支；同事需有仓库访问权限。
- [GitHub 完整代码](https://github.com/Leariceleto/eicc-2026-preview)，与 Gitee 同步。
- 本文件：平台适配的当前交接入口。
- `tests/`：模型行为测试，作为迁移后的回归参考，不代表已经通过真实接口或支付验收。

请拉取完整仓库或解压本次交付包，不能只拿 `index.html`。当前是原生 HTML / CSS / JavaScript，无 `package.json`，无构建或依赖安装步骤。

在项目根目录运行：

```bash
python3 -m http.server 4174 --bind 127.0.0.1
# 浏览器访问 http://127.0.0.1:4174/

node --test tests/account-model.test.cjs tests/passport-model.test.cjs
```

本次交接检查：28 项模型测试通过，新增征集入口已检查电脑和手机布局；这不替代平台内适配、接口、权限和支付测试。

| 文件 | 当前职责 |
| --- | --- |
| `index.html` | 公共官网、内联样式与脚本、报名弹窗、锚点路由、演示控制器、方案征集入口 |
| `assets/brand-visual.css` | 已确认的暖褐品牌视觉层；加载在其他样式之后，平台接入时需做组件级作用域隔离 |
| `assets/account.js`、`assets/account.css` | 一键查询、个人与经办工作区、订单及服务入口、未来币展示 |
| `assets/account-model.js` | 下单人／参会人关系和选课规则的演示模型，不是生产鉴权实现 |
| `assets/passport.js`、`assets/passport.css` | 学习护照四栏交互、记录、组织和成果界面 |
| `assets/passport-model.js` | 自评、路径、记录及报告的本地模型 |
| `assets/` 内图片 | 品牌字标、桌面及手机历程图、大会概况背景，需一并迁移 |
| `tests/*.test.cjs` | 身份关系、选课范围、学习路径和记录等回归测试 |

录音原稿、临时截图、`.tmp-*` 以及未追踪的内部工作笔记不属于本次前端交付包，不需要转发。

## 3. 先按这些入口看原型

线上地址后加下列后缀即可查看；这些参数只模拟数据，不是正式登录态。

| 预览场景 | 地址后缀 |
| --- | --- |
| 学习方式介绍（下方护照交互预览暂时隐藏） | `#passport` |
| 学习护照内部联调原型 | `?queryRole=both#account/path` |
| 新增学校方案征集 | `#expo` |
| 已登录未报名 | `?queryRole=none#account` |
| 下单人 | `?queryRole=buyer#account` |
| 仅参会人 | `?queryRole=attendee#account` |
| 下单人＋参会人 | `?queryRole=both#account` |
| 周期、票种与完成状态控制器 | `?demo=1` |
| 会前准备、组合权益样例 | `?demo=1&phase=prep&identity=combo&queryRole=both#account` |

一键查询内部另有可展开的“身份预览”；学习护照可切换完整样例和空白护照。演示状态、示例余额、订单、人员、座位、课程均不得作为生产数据。

## 4. 页面结构与不能遗漏的细节

公共页面依次为首屏、大会概况、票种权益、主论坛、深研课、方案展、学习方式介绍、页脚；学习方式下方的学习护照交互预览暂时隐藏。手机端历届年会图单独位于最后一个内容板块，桌面端仍在大会概况。`#guests` 目前隐藏，不要在迁移时自动恢复。

| 当前入口 | 适配重点 |
| --- | --- |
| `#about` | 保留历届年份与地点的跳转热区、既有图标和手机路线图；视频保持 16:9 |
| `#tickets`、立即报名 | 四类权益与报名流程；接平台登录、商品、报名、支付及订单结果 |
| `#agenda` | 主论坛按日期切换；保留已确认内容，不自行概括或新增嘉宾 |
| `#deep` | 保留导航“深研课”；分论坛与场外深研课在业务上分开处理 |
| `#expo` | 保留三张介绍卡与下方学校创新方案征集栏 |
| `#passport` | 保留公共学习方式介绍；下方整块学习护照交互预览暂不展示，完整设计并确认后再开放 |
| `#account` | 独立的一键查询页，不能只做成订单列表 |

四类参会方式保持顺序和名称：**主论坛、主论坛＋深研课、线上参会、独立方案展**。名称不追加“票”字；当前卡片价格为 2800 / 2900 / 1560 / 300 元，实际交易价格、优惠和库存由正式商品配置提供。

名称统一为“一键查询”“我的学习护照”“未来币”。总览的“我的未来币与兑换”区域不放充值按钮；收支与兑换等已有充值入口保留，各种已登录身份均可使用本人钱包充值入口。

## 5. 一键查询：先区分本人参会与经办事项

一键查询是当前账号全部有权获取的参会信息入口，包括报名权益、座位与入场、分论坛、场外深研课、酒店、学习护照、未来币、兑换、通知等。

| 身份 | 页面重点 | 权限边界 |
| --- | --- | --- |
| 已登录未报名 | 报名引导、本人钱包及公共服务 | 不虚构本人参会权益 |
| 下单人 | 经办订单、参会人列表、授权范围内的代办 | 下单不代表本人参会，也不能读取他人私人护照 |
| 仅参会人 | 本人权益、安排和学习档案 | 不因受邀参会获得整单支付、发票、退款权限 |
| 下单人＋参会人 | “我的参会”与“我经办的报名”两个工作区 | 切换经办对象不改变登录账号、本人护照或钱包 |

适配要求：

- 由服务端登录身份、本人有效权益和经办订单关系决定角色，不读取 `queryRole` 作为权限依据。
- 经办对象范围限定在当前账号有权办理的订单内；未支付、取消、退款订单可以保留查询，但不等于有效参会资格。
- 代选课前明确显示参会人、日期与场次，确认后再提交；不得替换另一个人或另一日的选择。
- 本人学习档案与钱包始终属于登录账号；下单人与同校关系都不自动授予私人记录访问权。
- 保留本人报名权益和经办订单两个入口，不把整个订单信息直接暴露给仅参会人。

## 6. 学习护照：本轮最重要的适配内容

**当前展示开关：** `index.html` 中 `[data-passport-home]` 带有 `hidden`，只隐藏首页嵌入区，包括工具栏、档案标题、四个栏目及其内容；桌面、手机和演示模式均不自动恢复。不要删除 `#passport-app` 或停用共享脚本，它仍供一键查询复用。完整设计完成并经业务确认后，才移除容器的 `hidden` 或打开平台对应展示开关。此开关只控制展示，不代替真实登录与权限隔离。

学习地图属于学习护照，不再独立建设一份不同步的学习记录。保留四个栏目：

1. **我的学习路径**：从真实问题和轻量自评形成个人路径，关联权益与可进入内容。主路径生成后保持不变，自由探索另行记录；推荐不代表已经选课或实际参加。
2. **学习任务与记录**：按日期／场景记录文字、照片和语音，区分主路径与自由学习；保留每日小结、方案整理等原型入口。
3. **我的学习组织**：普通班级与主题学习社区同层级、互斥归属，包含共同任务、知识接力及主动分享。
4. **学习成果**：个人报告基于真实保存的学习证据；同校伙伴汇总经授权的内容，共同生成专属于本校的 100 天行动计划。

上线边界：

- 现有护照使用浏览器 `localStorage`，键为 `eicc-passport-v1-` 加票种身份，**没有账号级存储隔离，不能直接用于多人真实登录**。正式版必须绑定活动、账号／参会人，接服务端持久化；切换账号不能带出上一个人的档案，示例数据不迁移为真实记录。
- 原型中的生成、整理、社区回应和报告不等于已接真实 AI 或多人协作。媒体上传、录音授权、转写、任务状态及异步结果需要平台与后端补齐。
- 私人记录须本人主动选择分享，才能进入组织共创或学校协作；学校归属不等于所有记录默认共享。
- 个人报告不能把推荐场次写成已参加，学校计划不能用演示文本冒充真实共同制定的结果。

## 7. 平台技术适配清单

- **组件与样式**：按平台现有框架拆分必要组件，保留目前米白／深色底、蓝色主按钮、字标、间距与卡片层级。`index.html` 含 `body`、`section`、`button` 等全局样式，不能原样注入平台全局；做命名空间或模块化隔离，避免影响平台其他页面。
- **路由**：把当前 hash 路由映射为平台约定路由，处理子目录资源路径、刷新直达、浏览器前进后退。公共区与一键查询互跳时恢复正确锚点，尤其“一键查询 → 学习方式”不能落到首页顶部。
- **统一登录**：复用平台账号与会话；未登录跳转登录后返回原目标，避免重复账号体系。前端按服务端返回的可用操作渲染，后端仍须逐次鉴权。
- **数据适配层**：将目前硬编码的商品、订单、选课、酒店、钱包、任务、回放和内容替换为真实数据；沿用平台接口规范，不据本原型另造一套接口协议。
- **真实状态**：补齐加载、空数据、无权限、处理中、失败重试、售罄／满额、未开放／已截止等状态。正式操作仅以服务端结果显示成功，防止重复提交。
- **支付及外跳返回**：报名支付、未来币充值、兑换均与后台结果联动。跳转既有业务页面时保留返回位置，返回后重新拉取权益、订单或余额，不能靠前端加减完成交易。
- **移动端与嵌入环境**：检查平台顶部栏／底部栏、安全区、弹窗滚动、表单键盘、历史图热区；手机不依赖左右拖动看全历程图。若目标包含 APP WebView／微信内页，另测外链、新窗口、回跳和媒体权限。
- **演示隔离**：保留独立测试环境供验收；正式版移除或隔离演示控制器、身份切换、样例初始化、模拟领取和模拟支付，不仅隐藏按钮。

## 8. 两个链接和视频预留位

| 项目 | 当前配置位置 | 约定行为 |
| --- | --- | --- |
| 学校创新方案征集 | `index.html` 中 `[data-expo-apply]` 的 `data-form-url=""` | 按钮文字为“我想加入”。填写学校基础信息与相关申请资料，后续由组委会沟通审核；不表示提交完整方案或审核通过 |
| 未来币充值 | `assets/account.js` 顶部 `platformRechargeUrl = ''` | 后续接一体化平台充值页，操作的是当前登录账号钱包；不随经办对象变化 |
| 历届回顾视频 | `index.html` 中 `data-video-src=""` | 补视频资源后启用，保持 16:9 与手机端播放支持 |

业务已明确：征集表单和充值页链接尚未确定，由后续技术接入。当前征集入口点击只显示待接入提示；填入有效 HTTPS 表单地址后在新窗口打开，不在本站收集资料。平台环境不支持新窗口时，按平台规范处理并提供返回路径。

## 9. 需要校准，不能照样例上线的内容

- 报名须知仍包含第十二届、2025—2026 回看日期、旧换人／退款边界；与当前票卡存在差异。由业务提供本届正式须知，再统一报名流程及后台配置，不能把这些样例当作已确认交易规则。
- 当前票卡顺序为主论坛、组合、线上、方案展，报名弹窗中线上与方案展顺序仍不同；正式版接统一商品顺序。
- 当前报名弹窗主要演示单人报名及支付／发票字段，经办工作区演示多人关系；真正的代报名／团报人员录入和订单衔接还需对接或补齐。
- 分论坛按“每人每天最多 1 场”，场外深研课按“每人最多 1 场”；两者不得共用一个总限额。价格、容量、换课、退款与截止时刻需正式配置，不能把演示模型当作完整库存或退改系统。
- 实际座位、场地、课程清单、酒店、客服、视频和播放器权益等以确认后的数据为准。当前示例展示不代表这些业务已可用。
- 正式自评题库、推荐内容映射、任务奖励及兑换商品由对应负责人提供；不得把原型数字直接作为奖励规则。

以上问题记录为联调清单，本次不擅自修改业务口径。

## 10. 适配验收与同事反馈

请前端同事先反馈：可复用的平台页面／组件、需新建或补齐的界面、所需接口与字段、待业务确认项，以及负责人和预计排期。

适配完成后至少验收：

- [ ] 公共内容与本版对齐；桌面及 375／390／430px 手机宽度无横向溢出，历程图及视频正常。
- [ ] 首页学习方式介绍保留，下方整块护照交互预览隐藏且不留空白；刷新、手机布局、演示模式和从一键查询返回时均不重新出现。一键查询内的护照原型仍可联调，正式开放需完整设计并确认。
- [ ] 立即报名、关闭／返回、日期切换、公共页面与一键查询互跳、刷新深层地址、前进后退均正常。
- [ ] 四种账号关系分别验证；本人和经办区域不串人，未支付／取消／退款不获得有效权益。
- [ ] 直接访问越权地址、修改 URL 角色或参会人 ID 也无法越权，不能只依赖隐藏按钮。
- [ ] 分论坛按日限额、场外独立限额；本人和代办查看的是同一真实结果，失败不覆盖原安排。
- [ ] 护照四栏、主路径固定、自由记录、主动分享与学校计划完整；换账号和跨设备不串数据。
- [ ] 报名支付、充值和兑换返回后正确刷新；加载／失败／处理中状态清楚，无伪成功。
- [ ] “我想加入”跳到正确申请表，外跳与返回可用；未配置时不给出虚假成功反馈。
- [ ] 生产入口无可切换身份、硬编码订单余额和模拟交易；控制台无影响使用的错误。

发布前由业务确认最终文案与交易规则，再完成前后端联合验收。此阶段结束的是本轮视觉和交互原型，不是整个系统开发与上线验收。

## 11. 首页主视觉比选（2026-09-09，设计预览）

**首屏展示要求（以此条为最新适配规则）：** 两个新版本的主视觉区域均使用 `100dvh`（`100vh` 回退），完整占满浏览器可视高度，不再设固定高度上限或预留底部空间。导航、按钮和倒计时位于主视觉内；比选条及其他内容从第二屏开始。手机长江黄河版改为 `object-fit:cover` 等比铺满，允许裁去两侧背景，保留标题和原图日期，避免上下出现纯色空带；按钮在原图日期上方。

- `?visual=planet`：星球版，电脑使用原稿横版，手机使用原稿第 3 页竖版。
- `?visual=river`：采用设计师最新的 `长江黄河版本（网站适配）.ai`。电脑使用第 5 页 2560×1440（16:9），手机使用第 4 页 1080×1920（9:16），以 760px 为切换断点。两版共用 `.key-visual-stage` 的满屏规则，确保常规电脑和手机首屏能看到两个按钮。PC、手机均等比覆盖（必要时裁去边缘背景），不拉伸、不重排图形和标题。电脑版日期、地点及按钮叠在图上，手机使用原稿日期、不重复添加，并避让按钮。两版倒计时均移入主视觉图底部，透明背景，不再保留独立底栏；手机倒计时使用紧凑单行，长江黄河版按钮位于原图日期上方。复用原计时节点，不复制计时器；切回原版时恢复原位置。
- `?visual=original`：原首页对照；不带 `visual` 参数时原首页保持不变。方案未最终选定，不将对比切换条作为正式用户功能。
- 实现集中在 `assets/key-visual.css`、`assets/key-visual.js` 和 `index.html` 的两个主视觉模板。图片为优化后的 WebP；长江黄河版改用 `kv-river-web-desktop.webp` 与 `kv-river-web-mobile.webp`，不再加载先前拆分的背景、SVG 或横幅裁切图。无需把大型 AI 源文件放入网站。
- 两个新主视觉版本的导航透明叠在图片顶部，PC、手机一致；滚动离开主视觉后恢复浅色底。一键查询页保持浅色导航，不受首页叠加布局影响。比选切换条放在主视觉及倒计时下方，仅供设计确认。
- 导航左侧统一使用 `assets/future-logo.png`，替代原金色“中国教育创新年会”文字标识。PC 显示高度 48px、手机 40px，宽度自适应；深色主视觉上保留原白色，浅色导航上用 CSS 转为黑色，源文件不变。
- 报名及学习方式按钮复用原有节点和事件，其他板块未重新设计；首页护照交互预览继续隐藏。本节说明随本轮代码一起交接，不代表正式业务系统上线。
